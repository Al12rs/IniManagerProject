<UserControl x:Class="INIManagerProject.EditListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:INIManagerProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border Background="GhostWhite" BorderBrush="Gainsboro" Margin="10,0,10,0" BorderThickness="1">
            <StackPanel>
                <ListView x:Name="lvEdit"  HorizontalAlignment="Stretch"  VerticalAlignment="Top" SelectionChanged="LvEdit_SelectionChanged">
                    <!--#LIST PROPERTY -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Center" ></Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <!--#endregion-->
                    <ListView.View>
                        <!--#region -->
                        <GridView>
                            <GridView.Columns>
                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox Width="15" IsEnabled="{Binding Path=IsEnabled}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="NAME" Width="300"  DisplayMemberBinding="{Binding EditName}"/>
                                <GridViewColumn Header="PRIORITY" Width="450" DisplayMemberBinding="{Binding EditPriority}"/>
                            </GridView.Columns>
                        </GridView>
                        <!--#endregion-->
                    </ListView.View>
                </ListView>
                <!--  <TextBlock  VerticalAlignment="Center" HorizontalAlignment="Center" >LISTA 1</TextBlock> -->
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
